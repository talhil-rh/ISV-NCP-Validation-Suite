# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# my-isv VM Validation Configuration - Living Example
#
# Imports validations from the provider-agnostic VM template and wires the
# commands to the generic template scripts in scripts/vm/*. Those scripts ship
# with dummy-success values so this example runs end-to-end without any
# real cloud infrastructure.
#
# Coverage:
#   This example exercises only the pure JSON-contract validations.
#   SSH-based host checks that need a real SSH-reachable node are
#   excluded via `exclude.labels: [ssh]` because a dummy-success stub
#   cannot spin up a real host to SSH into. The deploy_nim /
#   teardown_nim steps are also skipped for the same reason.
#
# ACTION: Remove `exclude.labels: [ssh]` at the bottom once your
#   stubs provision real instances with SSH access.
#
# Steps:
#   SETUP
#     1. launch_instance  - Provision a GPU VM
#   TEST
#     2. list_instances   - Find it in the VPC
#     3. verify_tags      - Verify user-defined tags
#     4. stop_instance    - Stop the VM
#     5. start_instance   - Start it back up
#     6. reboot_instance  - Reboot it
#     7. serial_console   - Confirm serial console access
#     8. console_rbac     - Verify console access is RBAC-scoped
#     9. component_key_access - AUTH03 specified-key SOL / network device access
#    10. virtual_device_hardening - Confirm unused virtio devices are absent
#    11. deploy_nim       - SKIPPED (needs real SSH)
#   TEARDOWN
#    12. teardown_nim     - SKIPPED (needs real SSH)
#    13. teardown         - Terminate the VM
#
# Usage:
#   uv run isvctl test run -f isvctl/configs/providers/my-isv/config/vm.yaml

import:
  - ../../../suites/vm.yaml

version: "1.0"

commands:
  vm:
    phases: ["setup", "test", "teardown"]
    steps:
      - name: launch_instance
        phase: setup
        command: "python ../scripts/vm/launch_instance.py"
        args:
          - "--name"
          - "isv-test-gpu"
          - "--instance-type"
          - "{{instance_type}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: list_instances
        phase: test
        command: "python ../scripts/vm/list_instances.py"
        args:
          - "--vpc-id"
          - "{{steps.launch_instance.vpc_id}}"
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: verify_tags
        phase: test
        command: "python ../scripts/vm/describe_tags.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: stop_instance
        phase: test
        command: "python ../scripts/vm/stop_instance.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: start_instance
        phase: test
        command: "python ../scripts/vm/start_instance.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
          - "--key-file"
          - "{{steps.launch_instance.key_file}}"
          - "--public-ip"
          - "{{steps.launch_instance.public_ip}}"
        timeout: 60

      - name: reboot_instance
        phase: test
        command: "python ../scripts/vm/reboot_instance.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
          - "--key-file"
          - "{{steps.launch_instance.key_file}}"
          - "--public-ip"
          - "{{steps.start_instance.public_ip}}"
        timeout: 60

      - name: serial_console
        phase: test
        command: "python ../scripts/vm/serial_console.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: console_rbac
        phase: test
        command: "python ../scripts/vm/console_rbac.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: component_key_access
        phase: test
        command: "python ../scripts/vm/component_key_access.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--key-file"
          - "{{steps.launch_instance.key_file}}"
          - "--key-name"
          - "{{steps.launch_instance.key_name}}"
          - "--region"
          - "{{region}}"
        timeout: 60

      - name: virtual_device_hardening
        phase: test
        command: "python ../scripts/vm/virtual_device_hardening.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
          - "--public-ip"
          - "{{steps.launch_instance.public_ip}}"
          - "--key-file"
          - "{{steps.launch_instance.key_file}}"
        timeout: 60

      # Skipped: deploy_nim and teardown_nim stubs SSH into a real host
      # to run a NIM container. The dummy-success stubs can't satisfy them.
      - name: deploy_nim
        phase: test
        skip: true
        command: "python ../../shared/deploy_nim.py"
        args:
          - "--host"
          - "{{steps.reboot_instance.public_ip}}"
          - "--key-file"
          - "{{steps.reboot_instance.key_file}}"
        timeout: 60

      - name: teardown_nim
        phase: teardown
        skip: true
        command: "python ../../shared/teardown_nim.py"
        args:
          - "--host"
          - "{{steps.reboot_instance.public_ip}}"
          - "--key-file"
          - "{{steps.reboot_instance.key_file}}"
        timeout: 60

      - name: teardown
        phase: teardown
        command: "python ../scripts/vm/teardown.py"
        args:
          - "--instance-id"
          - "{{steps.launch_instance.instance_id}}"
          - "--region"
          - "{{region}}"
          - "--delete-key-pair"
          - "--delete-security-group"
          - "{{teardown_flag}}"
        timeout: 60

tests:
  cluster_name: "my-isv-vm-validation"
  description: "my-isv VM lifecycle validation (generic stubs, dummy overrides)"

  settings:
    region: "my-isv-region-1"
    instance_type: "my-isv.gpu.1x"
    teardown_flag: "{{(env.VM_SKIP_TEARDOWN == 'true') | ternary('--skip-destroy', '')}}"

  # Skip SSH-based host checks (VmReachableOverSsh*, VmGpusPresent*,
  # VmSoftwareStackCheck, VmCpuInfoCheck, VmCloudInitCheck, NIM) - they'd try to connect
  # to the dummy public_ip. Pure JSON-contract checks still run.
  # Real ISVs remove this exclusion as their stubs come online.
  exclude:
    labels: ["ssh"]
